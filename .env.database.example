# 数据库配置（必须配置）
# SQLite 数据库连接字符串（本地开发）
DATABASE_URL="file:./prisma/dev.db"

# NextAuth 配置（必须配置）
# 使用以下命令生成随机密钥: openssl rand -base64 32
NEXTAUTH_SECRET="please-change-this-to-a-random-secret-key"
NEXTAUTH_URL="http://localhost:3000"
